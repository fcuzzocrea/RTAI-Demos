****** BITS EXAMPLE ******

This example demonstrates the use of a few basic services of rtai_bits in kernel
space. For more informations on rtai_bits services see the related README in
rtai-core/ipc/bits.

To run it do:

make
./run
